---
phase: 03-demo-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/career_os/models/models.py
  - src/career_os/fixtures/__init__.py
  - src/career_os/fixtures/demo_jobs.json
  - src/career_os/migration/demo_seed.py
  - alembic/versions/a1b2c3d4e5f6_add_is_demo_column.py
  - pyproject.toml
autonomous: true
requirements: [DEMO-01, DEMO-02, DEMO-03, DEMO-05]

must_haves:
  truths:
    - "10 demo Application rows can be created from the fixture file"
    - "Demo jobs span 7 job families (Tech, Marketing, Operations, Finance, Legal, Sales, Recruiting/HR)"
    - "Running the seeder twice produces exactly 10 records (not 20)"
    - "Seeded records have timestamps computed from days_ago offsets"
  artifacts:
    - path: "src/career_os/fixtures/demo_jobs.json"
      provides: "10 pre-scored sample jobs fixture"
      contains: "fit_score"
    - path: "src/career_os/migration/demo_seed.py"
      provides: "Idempotent demo seeder function"
      exports: ["seed_demo_data"]
    - path: "alembic/versions/a1b2c3d4e5f6_add_is_demo_column.py"
      provides: "Alembic migration for is_demo Boolean column"
      contains: "is_demo"
    - path: "src/career_os/fixtures/__init__.py"
      provides: "Package marker for importlib.resources"
  key_links:
    - from: "src/career_os/migration/demo_seed.py"
      to: "src/career_os/fixtures/demo_jobs.json"
      via: "importlib.resources.files"
      pattern: 'files\("career_os.fixtures"\)'
    - from: "src/career_os/migration/demo_seed.py"
      to: "src/career_os/models/models.py"
      via: "Application model with is_demo"
      pattern: "Application.is_demo"
---

<objective>
Create the demo data foundation: Alembic migration adding `is_demo` column, the JSON fixture file with 10 diverse pre-scored jobs, and the idempotent seeder module.

Purpose: All CLI integration (Plan 02) and auto-clear logic (Plan 03) depend on this foundation existing.
Output: Working seeder that can be called with `seed_demo_data(db, profile_id)` and produces 10 Application rows with `is_demo=True`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-demo-data/03-PATTERNS.md
@.planning/phases/03-demo-data/03-RESEARCH.md

<interfaces>
From src/career_os/models/models.py (lines 89-123):
```python
class Application(Base):
    __tablename__ = "applications"
    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    profile_id: Mapped[int] = mapped_column(Integer, ForeignKey(FK_PROFILES_ID), nullable=False, index=True)
    company: Mapped[str] = mapped_column(String(255), nullable=False)
    role: Mapped[str] = mapped_column(String(500), nullable=False)
    url: Mapped[str | None] = mapped_column(Text, nullable=True)
    source: Mapped[str | None] = mapped_column(String(100), nullable=True)
    status: Mapped[str] = mapped_column(String(50), nullable=False, default="discovered")
    salary_range: Mapped[str | None] = mapped_column(String(255), nullable=True)
    contact: Mapped[str | None] = mapped_column(String(255), nullable=True)
    next_step: Mapped[str | None] = mapped_column(Text, nullable=True)
    notes: Mapped[str | None] = mapped_column(Text, nullable=True)
    fit_score: Mapped[float | None] = mapped_column(Float, nullable=True)
    # ... timestamps and relationships follow
```

From src/career_os/migration/seed.py (pattern to follow):
```python
def seed_ghost_detection_records(db: Session, profile_id: int) -> int:
    existing = db.query(Application).filter(Application.source == "seed", Application.profile_id == profile_id).count()
    if existing > 0:
        return 0
    now = datetime.now(UTC)
    for record in GHOST_SEED_RECORDS:
        created_at = now - timedelta(days=record["days_ago"])
        app = Application(profile_id=profile_id, company=record["company"], ...)
        db.add(app)
    db.commit()
    return created
```

Latest migration revision: `q8r9s0t1u2v3` (down_revision for new migration)

pyproject.toml package-data section starts at line 63:
```toml
[tool.setuptools.package-data]
career_os = [
    "_frontend_dist/**/*",
    ...
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration + Application model is_demo column</name>
  <files>alembic/versions/a1b2c3d4e5f6_add_is_demo_column.py, src/career_os/models/models.py</files>
  <read_first>
    - src/career_os/models/models.py (lines 89-123 — Application class, see where archived_at ends at line 115)
    - alembic/versions/q8r9s0t1u2v3_add_profile_salary_experience.py (latest migration — copy structure exactly)
  </read_first>
  <action>
1. Create `alembic/versions/a1b2c3d4e5f6_add_is_demo_column.py`:
   - revision = "a1b2c3d4e5f6"
   - down_revision = "q8r9s0t1u2v3"
   - upgrade: `op.batch_alter_table('applications', schema=None)` then `batch_op.add_column(sa.Column('is_demo', sa.Boolean(), server_default=sa.text("0"), nullable=False))`
   - downgrade: `op.batch_alter_table('applications', schema=None)` then `batch_op.drop_column('is_demo')`
   - Import pattern: `from typing import Sequence, Union`, `from alembic import op`, `import sqlalchemy as sa`

2. Modify `src/career_os/models/models.py` — add after `archived_at` (line 115), before the `# Relationships` comment (line 117):
   ```python
   is_demo: Mapped[bool] = mapped_column(Boolean, default=False, server_default=text("0"), nullable=False)
   ```
   Also add `text` to the sqlalchemy imports at the top if not already present (check line 3-7 for existing imports like `from sqlalchemy import ...`). Add `Boolean` to imports if not present.
  </action>
  <verify>
    <automated>cd /Users/pleasedodisturb/Projects/kestrel-onboarding && python -c "from career_os.models.models import Application; assert hasattr(Application, 'is_demo')" && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - alembic/versions/a1b2c3d4e5f6_add_is_demo_column.py exists with `revision = "a1b2c3d4e5f6"` and `down_revision = "q8r9s0t1u2v3"`
    - models.py contains `is_demo: Mapped[bool] = mapped_column(Boolean, default=False, server_default=text("0"), nullable=False)`
    - models.py imports include `Boolean` and `text`
    - Migration uses `batch_alter_table` (required for SQLite)
  </acceptance_criteria>
  <done>is_demo column defined in ORM model and Alembic migration ready to apply</done>
</task>

<task type="auto">
  <name>Task 2: JSON fixture file + fixtures package + pyproject.toml update</name>
  <files>src/career_os/fixtures/__init__.py, src/career_os/fixtures/demo_jobs.json, pyproject.toml</files>
  <read_first>
    - pyproject.toml (lines 63-70 — package-data section to add fixtures/*.json)
    - src/career_os/migration/seed.py (lines 21-46 — GHOST_SEED_RECORDS structure for field reference)
  </read_first>
  <action>
1. Create `src/career_os/fixtures/__init__.py` — empty file (required for importlib.resources).

2. Create `src/career_os/fixtures/demo_jobs.json` with exactly 10 jobs per D-05/D-06/D-07/D-08/D-09:

```json
[
  {
    "company": "Vantage Analytics GmbH",
    "role": "Senior Data Engineer",
    "job_family": "Tech",
    "location": "Berlin, Remote",
    "salary_range": "EUR 75,000-95,000",
    "fit_score": 91.2,
    "days_ago": 2,
    "source_board": "LinkedIn",
    "notes": "Series B data platform. Berlin, Remote. Via LinkedIn."
  },
  {
    "company": "NovaPay Solutions",
    "role": "Growth Marketing Lead",
    "job_family": "Marketing",
    "location": "Amsterdam, Hybrid",
    "salary_range": "EUR 65,000-80,000",
    "fit_score": 72.4,
    "days_ago": 3,
    "source_board": "Indeed",
    "notes": "Series B fintech. Amsterdam, Hybrid. Via Indeed."
  },
  {
    "company": "Meridian Legal Partners",
    "role": "Corporate Counsel, Tech Transactions",
    "job_family": "Legal",
    "location": "Amsterdam, Hybrid",
    "salary_range": "EUR 85,000-110,000",
    "fit_score": 32.5,
    "days_ago": 5,
    "source_board": "Indeed",
    "notes": "International law firm, GDPR specialization. Amsterdam, Hybrid. Via Indeed."
  },
  {
    "company": "Apex Freight Logistics",
    "role": "Operations Manager, EU Distribution",
    "job_family": "Operations",
    "location": "Rotterdam, On-site",
    "salary_range": "EUR 60,000-75,000",
    "fit_score": 55.8,
    "days_ago": 1,
    "source_board": "LinkedIn",
    "notes": "Mid-market logistics. Rotterdam, On-site. Via LinkedIn."
  },
  {
    "company": "Birchwood Capital",
    "role": "Financial Analyst, Private Equity",
    "job_family": "Finance",
    "location": "London, Hybrid",
    "salary_range": "GBP 55,000-70,000",
    "fit_score": 63.1,
    "days_ago": 4,
    "source_board": "Glassdoor",
    "notes": "PE fund, mid-market focus. London, Hybrid. Via Glassdoor."
  },
  {
    "company": "Stellar Cloud Inc.",
    "role": "Enterprise Account Executive",
    "job_family": "Sales",
    "location": "Dublin, Remote",
    "salary_range": "EUR 70,000-90,000 + commission",
    "fit_score": 48.7,
    "days_ago": 6,
    "source_board": "LinkedIn",
    "notes": "B2B SaaS, EMEA territory. Dublin, Remote. Via LinkedIn."
  },
  {
    "company": "TalentBridge Group",
    "role": "Senior Technical Recruiter",
    "job_family": "Recruiting/HR",
    "location": "Munich, Hybrid",
    "salary_range": "EUR 55,000-68,000",
    "fit_score": 27.3,
    "days_ago": 7,
    "source_board": "Indeed",
    "notes": "Recruitment agency, tech vertical. Munich, Hybrid. Via Indeed."
  },
  {
    "company": "Quantum Robotics Ltd",
    "role": "Senior Backend Engineer (Python)",
    "job_family": "Tech",
    "location": "London, Remote",
    "salary_range": "GBP 80,000-100,000",
    "fit_score": 88.6,
    "days_ago": 1,
    "source_board": "LinkedIn",
    "notes": "Series C robotics startup. London, Remote. Via LinkedIn."
  },
  {
    "company": "Greenfield Ventures",
    "role": "Head of Content Marketing",
    "job_family": "Marketing",
    "location": "New York, Hybrid",
    "salary_range": "USD 95,000-120,000",
    "fit_score": 59.4,
    "days_ago": 3,
    "source_board": "Glassdoor",
    "notes": "Climate-tech VC fund. New York, Hybrid. Via Glassdoor."
  },
  {
    "company": "ClearPoint Advisory",
    "role": "FP&A Manager",
    "job_family": "Finance",
    "location": "Frankfurt, On-site",
    "salary_range": "EUR 70,000-85,000",
    "fit_score": 41.2,
    "days_ago": 5,
    "source_board": "StepStone",
    "notes": "Management consulting. Frankfurt, On-site. Via StepStone."
  }
]
```

Score distribution per D-07: High (91.2, 88.6), Mid (72.4, 63.1, 59.4, 55.8, 48.7, 41.2), Low (32.5, 27.3).
Job families per D-05: Tech(2), Marketing(2), Legal(1), Operations(1), Finance(2), Sales(1), Recruiting/HR(1) = 7 families.
Markets per D-08: EU majority (Berlin, Amsterdam x2, Rotterdam, Dublin, Munich, Frankfurt = 7), UK (London x2 = 2), US (New York = 1). EU emphasis achieved.
Currencies per D-09: EUR (7), GBP (2), USD (1).

3. Modify `pyproject.toml` — add `"fixtures/*.json",` to the `career_os` package-data list (after the existing `_alembic.ini` entry).
  </action>
  <verify>
    <automated>cd /Users/pleasedodisturb/Projects/kestrel-onboarding && python -c "import json; from importlib.resources import files; data = json.loads(files('career_os.fixtures').joinpath('demo_jobs.json').read_text()); assert len(data) == 10; families = set(j['job_family'] for j in data); assert len(families) == 7; print(f'OK: {len(data)} jobs, {len(families)} families')"</automated>
  </verify>
  <acceptance_criteria>
    - src/career_os/fixtures/__init__.py exists (can be empty)
    - src/career_os/fixtures/demo_jobs.json contains exactly 10 objects
    - Each object has fields: company, role, job_family, location, salary_range, fit_score, days_ago, source_board, notes
    - 7 unique job_family values present
    - fit_score values: 2 in 85-95 range, 6 in range 40-75, 2 in 20-40 range
    - pyproject.toml contains "fixtures/*.json" in package-data section
  </acceptance_criteria>
  <done>Fixture file loadable via importlib.resources, 10 diverse jobs with correct score distribution and EU market emphasis</done>
</task>

<task type="auto">
  <name>Task 3: Demo seeder module (seed_demo_data function)</name>
  <files>src/career_os/migration/demo_seed.py</files>
  <read_first>
    - src/career_os/migration/seed.py (full file — follow import style, logging, function signature pattern exactly)
    - src/career_os/fixtures/demo_jobs.json (created in Task 2 — verify field names match)
  </read_first>
  <action>
Create `src/career_os/migration/demo_seed.py` following the exact pattern of `seed.py`:

```python
"""Seed demo job data for onboarding aha-moment."""

import json
import logging
from datetime import UTC, datetime, timedelta
from importlib.resources import files

from sqlalchemy.orm import Session

from career_os.models.models import Application

logger = logging.getLogger(__name__)


def seed_demo_data(db: Session, profile_id: int) -> int:
    """Seed demo jobs from fixture. Idempotent: delete-then-insert (per D-04).

    Returns:
        Number of records created.
    """
    # D-04: Delete existing demo records first (idempotent replace)
    deleted = (
        db.query(Application)
        .filter(
            Application.is_demo.is_(True),
            Application.profile_id == profile_id,
        )
        .delete()
    )
    if deleted > 0:
        logger.info("Deleted %d existing demo records before re-seeding", deleted)

    # Load fixture via importlib.resources (works in editable and wheel installs)
    fixture_path = files("career_os.fixtures").joinpath("demo_jobs.json")
    jobs = json.loads(fixture_path.read_text(encoding="utf-8"))

    now = datetime.now(UTC)
    created = 0
    for job in jobs:
        created_at = now - timedelta(days=job["days_ago"])
        app = Application(
            profile_id=profile_id,
            company=job["company"],
            role=job["role"],
            source="demo",
            status="discovered",
            fit_score=job["fit_score"],
            salary_range=job["salary_range"],
            notes=job.get("notes", ""),
            is_demo=True,
            created_at=created_at,
            updated_at=created_at,
        )
        db.add(app)
        created += 1

    db.commit()
    logger.info("Seeded %d demo job records", created)
    return created
```

Key points:
- Uses delete-then-insert (D-04), NOT skip-if-existing like ghost seeds
- Filters on `Application.is_demo.is_(True)` (D-12), NOT `source == "demo"`
- Computes `created_at` from `days_ago` offset (D-10)
- Sets `is_demo=True` on all records (D-12)
- Returns count of records created
  </action>
  <verify>
    <automated>cd /Users/pleasedodisturb/Projects/kestrel-onboarding && python -c "from career_os.migration.demo_seed import seed_demo_data; print('Import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - src/career_os/migration/demo_seed.py exists
    - Contains `def seed_demo_data(db: Session, profile_id: int) -> int:`
    - Uses `Application.is_demo.is_(True)` filter for deletion
    - Uses `files("career_os.fixtures").joinpath("demo_jobs.json")` for fixture loading
    - Sets `is_demo=True` on every created Application
    - Computes `created_at = now - timedelta(days=job["days_ago"])`
    - Calls `db.commit()` at the end
  </acceptance_criteria>
  <done>seed_demo_data function importable and ready to be called by CLI commands</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture JSON -> ORM | Trusted data (version-controlled, shipped with package) crosses into DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | demo_jobs.json | accept | Fixture is version-controlled in git; any tampering requires repo write access which already implies full system access |
| T-03-02 | Tampering | SQL injection via fixture fields | mitigate | SQLAlchemy ORM parameterized queries — all fixture values pass through mapped_column types, never raw SQL |
| T-03-03 | Denial of Service | Repeated seeder calls | accept | Delete-then-insert is O(10) records; negligible performance impact even if called thousands of times |
</threat_model>

<verification>
- `python -c "from career_os.migration.demo_seed import seed_demo_data"` succeeds
- `python -c "from career_os.models.models import Application; assert hasattr(Application, 'is_demo')"` succeeds
- `python -c "import json; from importlib.resources import files; assert len(json.loads(files('career_os.fixtures').joinpath('demo_jobs.json').read_text())) == 10"`
</verification>

<success_criteria>
- is_demo Boolean column exists on Application model with migration
- 10-job fixture file loadable via importlib.resources
- seed_demo_data function importable and has correct delete-then-insert logic
- pyproject.toml includes fixtures/*.json in package-data
</success_criteria>

<output>
After completion, create `.planning/phases/03-demo-data/03-01-SUMMARY.md`
</output>
