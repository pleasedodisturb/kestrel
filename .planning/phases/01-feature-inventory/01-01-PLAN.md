---
phase: 01-feature-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/roadmap/inventory.md
autonomous: false
requirements:
  - INV-01
  - INV-02
  - INV-03
  - INV-04
  - INV-05
  - INV-06
  - INV-07
  - INV-08

must_haves:
  truths:
    - "A reader can see all 8 shipped domains (Scoring Engine, Discovery Engine, AI Provider System, Application Pipeline, Web Frontend, CLI & Packaging, Infrastructure, Integrations) in one document without reading source code"
    - "Voice mode and Mobile app appear in a distinct 'Parked Work' section — not mixed into shipped features"
    - "An opening evolution narrative (2-3 paragraphs) tells how Kestrel grew from CLI scoring tool to full-stack platform"
    - "Every domain section contains a concise bullet list of shipped capabilities plus inline honest gaps about user-facing limitations"
    - "A summary table at the top lists all domains with key capabilities and Shipped/Parked status for quick scanning"
    - "Each domain section includes CHANGELOG.md release tag cross-references"
    - "The inventory is written in warm teaching tone — explains what each domain does for a job seeker, not internal architecture"
  artifacts:
    - path: "docs/roadmap/inventory.md"
      provides: "Complete feature inventory document"
      min_lines: 200
    - path: "docs/roadmap/"
      provides: "Directory for roadmap docs (Phase 4 will add more files here)"
  key_links:
    - from: "docs/roadmap/inventory.md"
      to: "CHANGELOG.md"
      via: "release tag anchor links"
      pattern: "CHANGELOG\\.md#"
    - from: "docs/roadmap/inventory.md"
      to: "Parked Work section"
      via: "Mobile and Voice subsections"
      pattern: "## Parked Work"
---

<objective>
Write docs/roadmap/inventory.md — a warm, honest, user-facing catalogue of every shipped Kestrel
capability, organized into 8 domain clusters plus a Parked Work section, with an opening evolution
narrative and summary table.

Purpose: This document is the raw material for Phase 2's ROADMAP.md assembly. It catalogues
what Kestrel has actually shipped so the master roadmap can present that history accurately
and compellingly. A job seeker evaluating Kestrel should be able to read this and understand
what the product does today, without reading code or docs.

Output: docs/roadmap/inventory.md (~2,000–3,000 words)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-feature-inventory/01-CONTEXT.md
@.planning/phases/01-feature-inventory/01-RESEARCH.md
</context>

<critical_corrections>
## IMPORTANT: Research Doc Has Stale Information

The 01-RESEARCH.md file was written before v0.11.0 shipped. You MUST apply these corrections
when writing the inventory — do not use the research doc's framing uncritically:

### Correction 1: v0.11.0 is the latest release (not v0.10.0)

v0.11.0 (2026-04-21) shipped a major set of features. These are properly CHANGELOG-documented:

- **G-392:** Onboarding Epic — 6 phases, 361 tests
- **G-439:** Pre-filter integrated into discovery pipeline
- **G-440:** Batch scoring — multiple jobs per prompt
- **G-441:** Prompt caching for scoring calls
- **G-442:** Cost presets system (Free/Budget/Quality/Private/Custom)
- **G-443:** Anthropic + OpenAI async Batch APIs (50% off scoring)
- **G-445:** Groq provider (OpenAI-compatible)
- **G-446:** OpenAI direct provider
- **G-447:** xAI/Grok provider with red privacy tier
- **G-448:** Google Gemini AI provider
- **G-449:** Provider privacy disclosures in AI Providers panel
- **G-450:** Kestrel MCP server for Claude Code
- **G-451:** OpenRouter OAuth PKCE onboarding flow

### Correction 2: All 9 providers are properly CHANGELOG-documented

The research doc says "Groq, xAI, OpenAI, Gemini exist in code but have no CHANGELOG release
notes." This was true at research time. It is NO LONGER TRUE. All 4 shipped in v0.11.0 (G-445,
G-446, G-447, G-448). Write the AI Provider System domain describing all 9 providers as shipped
and documented. The "Pitfall 1" caveat in the research doc is outdated — do NOT apply it.

### Correction 3: Cost presets shipped in v0.11.0 (G-442)

The cost presets system (Free/Budget/Quality/Private/Custom) is a shipped feature in v0.11.0.
Add it to the AI Provider System domain or Scoring Engine as appropriate (AI Provider System
is the right primary home — it controls which provider + model + batch size is used).

### Correction 4: CHANGELOG anchor format

CHANGELOG.md headings are formatted as `## [0.11.0](url)`. GitHub strips brackets and dots
when generating anchor IDs. The anchor for `## [0.11.0]` is `#0110`. The anchor for
`## [0.3.0]` is `#030`. Full correct examples:
- `[v0.11.0](CHANGELOG.md#0110)`
- `[v0.10.0](CHANGELOG.md#0100)` (note: v0.10.0 heading exists as `## [0.10.0]` in the file)
- `[v0.3.0](CHANGELOG.md#030)`
- `[v0.2.0](CHANGELOG.md#020)`
- `[v0.1.0](CHANGELOG.md#010)`

Do NOT use `#v030` or `#0-3-0`. Use the format above.
</critical_corrections>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/roadmap/ directory</name>
  <files>docs/roadmap/</files>
  <read_first>
    - Run `ls /Users/pleasedodisturb/Projects/kestrel/docs/roadmap/ 2>/dev/null || echo "DOES NOT EXIST"` to confirm directory is absent before creating
  </read_first>
  <action>
    Create the docs/roadmap/ directory. This directory does not exist yet (confirmed).
    Run: `mkdir -p docs/roadmap/`

    This is a prerequisite for Task 2. Do not write inventory.md until this directory exists.
    Verify with: `test -d docs/roadmap && echo "OK"`
  </action>
  <verify>
    <automated>test -d /Users/pleasedodisturb/Projects/kestrel/docs/roadmap && echo "directory exists"</automated>
  </verify>
  <done>docs/roadmap/ directory exists on disk</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 creates the docs/roadmap/ directory. This checkpoint is a lightweight
    quality gate before the main writing task begins. The inventory content is
    assembled below — review it before the executor writes the file.
  </what-built>
  <how-to-verify>
    Confirm you want the executor to write docs/roadmap/inventory.md with:
    - 8 domain sections in this order: Scoring Engine, Discovery Engine, AI Provider System,
      Application Pipeline, Web Frontend, CLI & Packaging, Infrastructure, Integrations
    - Parked Work section (Mobile + Voice)
    - Opening evolution narrative (2-3 paragraphs, warm teaching tone)
    - Summary table at top
    - Warm tone throughout (~150-250 words per domain, 2,000-3,000 words total)
    - All 9 AI providers listed (including Groq, xAI, OpenAI, Gemini from v0.11.0)
    - Inline honest gaps (user-impact only — no internal tech debt)
    - CHANGELOG.md cross-links for each domain

    If you want to adjust anything before the write proceeds, now is the time.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe adjustments needed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write docs/roadmap/inventory.md</name>
  <files>docs/roadmap/inventory.md</files>
  <read_first>
    Before writing, read these source files to ensure the inventory content is accurate
    and current. Do NOT rely solely on the research doc — it has known-stale sections
    (see Critical Corrections above):

    1. `.planning/codebase/ARCHITECTURE.md` — system layers, entry points, AI provider abstraction
    2. `.planning/codebase/STRUCTURE.md` — module counts, page list, file-level feature verification
    3. `.planning/codebase/INTEGRATIONS.md` — external services, auth patterns, notification systems
    4. `.planning/codebase/STACK.md` — framework versions, dependency list
    5. `CHANGELOG.md` — read the first 100 lines to confirm v0.11.0 release contents
    6. `docs/reference/AI-PROVIDERS.md` — user-facing provider descriptions
    7. `docs/reference/DEPLOY.md` — deployment paths and known gaps
  </read_first>
  <action>
    Write docs/roadmap/inventory.md using the structure and content specified below.
    Every claim must be traceable to a source doc — do not invent capabilities.

    ---

    ## FILE STRUCTURE

    The file must follow this exact structure:

    1. Title + metadata
    2. Summary table (per D-13)
    3. Opening evolution narrative (per D-06)
    4. 8 domain sections in order: Scoring Engine, Discovery Engine, AI Provider System,
       Application Pipeline, Web Frontend, CLI & Packaging, Infrastructure, Integrations
    5. Parked Work section (per D-02)

    ---

    ## TITLE AND METADATA

    ```markdown
    # Kestrel Feature Inventory

    > A catalogue of everything shipped in Kestrel through v0.11.0. Written for job seekers
    > evaluating the product and contributors looking for context. Updated: 2026-04-22.
    ```

    ---

    ## SUMMARY TABLE (D-13)

    Write this table immediately after the title block:

    ```markdown
    ## At a Glance

    | Domain | Key Capabilities | Status |
    |--------|-----------------|--------|
    | Scoring Engine | AI scoring, letter grades (A+–F), red flags, feedback loops, 288 presets, dual fit/desire scores | Shipped |
    | Discovery Engine | Multi-board job scraping, auto-schedule, deduplication, WARN Act data | Shipped |
    | AI Provider System | 9 providers (Mock through Gemini), cost presets, PII masking, encrypted cache, privacy registry | Shipped |
    | Application Pipeline | Kanban stages, state machine, contacts, interview prep, STAR stories, gap analysis | Shipped |
    | Web Frontend | 13 pages, drag-and-drop Kanban, analytics charts, discovery feed, onboarding wizard | Shipped |
    | CLI & Packaging | `kestrel` CLI, pip/Docker/Homebrew/npx install paths, auto-migration | Shipped |
    | Infrastructure | GitHub Actions CI/CD, 129 tests, security scanning, privacy layer, encrypted cache | Shipped |
    | Integrations | TickTick, Pushover, Calendar export, OpenRouter OAuth, Cloudflare Worker | Shipped |
    | Mobile App | React Native/Expo 54 scaffold, Tamagui UI, UX research preserved | Parked |
    | Voice Mode | API endpoint + frontend page exist, end-to-end flow untested | Parked |
    ```

    ---

    ## OPENING EVOLUTION NARRATIVE (D-06, INV-08)

    Write 2-3 paragraphs in warm teaching tone. Use this confirmed arc from CHANGELOG.md:

    **Paragraph 1 — The beginning:** Kestrel started as a command-line scoring tool. Before the
    first public release, the core loop was already there: feed it a job description, get a score
    back. The v0.1.0 release established the foundation — a backend API, a web frontend, and a
    basic pipeline for tracking jobs from "discovered" to "offer." But the scoring was simple,
    there was only one AI backend, and the interface was very much a developer's tool.

    **Paragraph 2 — Scoring depth + multi-provider:** Between v0.2.0 and v0.5.0, two things
    happened in parallel. Scoring got much smarter: letter grades, red flag detection, dual
    fit/desire scores, Bayesian preference learning, 288 job family presets across 16 sectors,
    borderline re-scoring, uncertainty ranges for sparse profiles. At the same time, Kestrel grew
    from a single-AI-backend system into a pluggable provider platform — Mock for offline use,
    Anthropic for best quality, Ollama for full local privacy, OpenRouter for access to 200+
    models, Together for open-source hosting. PII masking, provider privacy disclosures, and an
    encrypted response cache shipped alongside the providers.

    **Paragraph 3 — Cost control + hardening + v0.11.0:** From v0.6.0 through v0.11.0, the focus
    shifted to cost control, reliability, and making Kestrel approachable. Batch scoring, prompt
    caching, complexity-tier routing, provider fallback chains, and an async batch API reduced
    AI costs to under a dollar a month at typical usage. Four new providers shipped in v0.11.0
    (Groq, OpenAI direct, xAI/Grok, Gemini), along with cost presets that configure the whole
    AI stack in one setting. An onboarding wizard (6 phases, 361 tests) replaced the "read the
    README to get started" experience. CI hardening, security scanning, and advanced test
    infrastructure phases ran parallel throughout — today Kestrel has 129+ automated tests, a
    comprehensive CI pipeline, and SonarCloud quality gates.

    Use your own words for the actual prose — this is a content brief, not a script. The tone
    should feel like a knowledgeable friend explaining the product, not a press release.

    ---

    ## DOMAIN SECTIONS (D-01, D-03, D-04, D-05, D-07, D-08, D-09, D-10)

    For each domain, write in this format:
    - H2 section heading: `## [Domain Name]`
    - 1-2 paragraphs of warm prose explaining what this domain does for a job seeker and why it
      matters. Prose should be 80-150 words.
    - `**What's shipped:**` heading followed by a bullet list of concrete capabilities
    - `*Released across [vX.Y.Z](CHANGELOG.md#XYZ), [vA.B.C](CHANGELOG.md#ABC)*` line
    - If there are user-facing gaps, weave them into a final paragraph or note inline in the
      relevant bullet item. Do NOT create a separate "Gaps" heading.

    Rules per user decisions:
    - D-07: ~150-250 words per domain section total (prose + bullets)
    - D-08: Release tags only — NO file paths, API routes, or architecture details
    - D-09: Inline honest assessment — gaps in prose, not a separate section
    - D-10: User-impact gaps ONLY — skip internal tech debt (no scoring monolith, no pip lockfile,
      no sync clients)

    ### Domain 1: Scoring Engine

    **User-facing description to convey in prose:** The scoring engine is the brain of Kestrel —
    it reads job descriptions and your profile, then returns a score from 0–10 with a letter
    grade (A+ through F), a breakdown by dimension, and a list of red flags. The more you use
    it and correct its mistakes, the better it learns what you actually want.

    **Bullets to include (derive exact wording from source docs):**
    - AI-powered multi-factor scoring with profile-driven weights (skills, experience, job family, location, preferences)
    - Letter grade system A+ through F with score percentiles relative to your personal history
    - Red flag detection: vague responsibilities, hidden employer, salary-free postings, ghost job indicators
    - Dual-score architecture: "fit" (how well you match) vs. "desire" (how much you want it)
    - Borderline re-scoring: close calls get a second pass and results are averaged
    - User feedback loop: mark a score wrong, the system learns your actual preferences
    - Bayesian preference learning from historical feedback signals
    - Uncertainty ranges for sparse profiles: admits when it doesn't have enough data
    - 288 job family weight presets across 16+ sectors: sensible defaults for any job type
    - Score context and percentiles: where does this job rank against everything you've seen?
    - Scoring calibration rubric with few-shot examples anchors consistency
    - ESCO skill taxonomy normalization maps free-text skills to a standard vocabulary
    - Keyword-based pre-filter eliminates obviously-wrong jobs before the AI sees them
    - Hard caps and keyword exemption system for edge cases

    **Honest gap to weave in:** Scoring quality depends on profile completeness — sparse profiles
    produce wider uncertainty ranges, not wrong scores. No score history chart in the UI yet
    (individual scores are visible but not trending over time).

    **Release anchors:** [v0.2.0](CHANGELOG.md#020), [v0.4.0](CHANGELOG.md#040),
    [v0.6.0](CHANGELOG.md#060), [v0.10.0](CHANGELOG.md#0100), [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 2: Discovery Engine

    **User-facing description to convey in prose:** The discovery engine finds job postings from
    multiple job boards automatically. Set your search parameters once, and Kestrel keeps the
    queue fresh on a schedule — no manual searching required.

    **Bullets to include:**
    - Multi-board job scraping via adapter abstraction (each board independently isolated)
    - Adapter fault isolation: one board failing doesn't block results from others
    - Background scheduling: auto-discovery runs on a regular interval
    - Manual trigger: kick off a fresh discovery run from the web UI at any time
    - Saved search parameters per profile
    - Deduplication: jobs you've already seen don't re-appear as new discoveries
    - "New matches" banner in the Discovery page when fresh results arrive
    - Job family keyword presets guide discovery scope
    - WARN Act layoff data integration (optional extra): surfaces companies with recent layoffs
    - Pre-filter integration (v0.11.0): discovered jobs pre-screened before scoring to reduce cost

    **Honest gaps to weave in:** Discovery depends on job boards making data available for
    scraping — boards that block scrapers or require login won't be covered. A planned browser
    extension will address this. The app must be running to check for new jobs on schedule.

    **Release anchors:** [v0.2.0](CHANGELOG.md#020), [v0.6.0](CHANGELOG.md#060),
    [v0.7.0](CHANGELOG.md#070), [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 3: AI Provider System

    **User-facing description to convey in prose:** Kestrel works with 9 different AI services —
    you choose the one that fits your privacy needs, budget, and technical comfort level. Switch
    providers at any time without losing data or history. Cost presets (Free/Budget/Quality/
    Private/Custom) configure the full AI stack — provider, model, pre-filter aggressiveness,
    and batch size — in a single setting.

    **Bullets to include:**
    - Pluggable provider system: switch AI backend via one environment setting or cost preset
    - **Mock (Demo Mode):** works offline, no API key or signup needed — explore Kestrel before committing
    - **OpenRouter:** one key, access to Claude, GPT, and 200+ models; browser-based OAuth setup
    - **Anthropic (direct):** best prompt caching economics; 7-day data retention policy
    - **Ollama:** fully local, nothing leaves your machine, free
    - **Together AI:** open-source model hosting, Frankfurt region
    - **Groq, OpenAI (direct), xAI/Grok, Gemini:** four providers added in v0.11.0 covering inference speed, standard models, Grok access, and Google's latest
    - Cost presets (Free/Budget/Quality/Private/Custom): one setting configures provider, model, batch size, and pre-filter aggressiveness
    - Complexity-tier routing: simple tasks use cheaper models, deep analysis uses capable ones automatically
    - Async Batch API scoring: overnight runs at 50% cost via Anthropic/OpenAI batch APIs
    - Token usage tracking: see exactly how much you spend per operation
    - Encrypted AI response cache: identical requests return cached results, 7-day TTL
    - PII masking before data leaves your machine: emails, URLs, phone numbers stripped from prompts
    - Provider privacy registry: factual disclosure of each provider's data retention, GDPR status, training policy, with color-coded trust tiers
    - Provider privacy disclosures visible in the AI Providers integration panel
    - Provider fallback chain: if primary provider fails, retries on backup automatically
    - System prompt compression: 67% token reduction on base prompts
    - Robust JSON parsing with retry: AI response malformations handled gracefully
    - Kestrel MCP server for Claude Code (v0.11.0)

    **Honest gaps to weave in:** Mistral (EU-sovereign, GDPR-native) is documented as coming
    soon but not yet shipped. Provider selection involves understanding data retention tradeoffs —
    the privacy registry makes these visible but the decision is still yours to make.

    **Release anchors:** [v0.3.0](CHANGELOG.md#030), [v0.4.0](CHANGELOG.md#040),
    [v0.5.0](CHANGELOG.md#050), [v0.8.0](CHANGELOG.md#080), [v0.9.0](CHANGELOG.md#090),
    [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 4: Application Pipeline

    **User-facing description to convey in prose:** The pipeline tracks every job you're pursuing,
    from first discovery to offer accepted or ghosted. Each application moves through stages with
    an enforced workflow, an automatic activity log, and follow-up reminders so nothing falls
    through the cracks. The v0.11.0 onboarding wizard (6 phases, 361 tests) makes the pipeline
    approachable for new users out of the box.

    **Bullets to include:**
    - Full application lifecycle: Discovered → Interested → Applied → Interviewing → Offer → Accepted/Rejected/Ghosted
    - Enforced state transitions: can't accidentally skip stages
    - Activity log: every status change and manual note timestamped automatically
    - Follow-up reminders with configurable timing
    - Contact tracking: who you talked to, what was discussed, when to follow up
    - Ghost job detection: automated flag when a posting goes dark
    - STAR story builder: structure interview narratives using the STAR method
    - Interview prep: tailored question sets and coaching for specific roles
    - Company research integration: pull intelligence on target companies
    - Gap analysis: what skills and experience delta this role represents
    - Calendar integration: export interview dates to iCal, Google Calendar, Fantastical
    - Goals tracking: record what you want from your next role
    - Onboarding wizard: guides new users through profile setup and first job search (v0.11.0)

    **Honest gaps to weave in:** No email or calendar integration for auto-capturing interview
    invites — you add them manually. Kestrel doesn't submit applications on your behalf; it's a
    tracking and intelligence layer, not an application bot.

    **Release anchors:** [v0.1.0](CHANGELOG.md#010), [v0.2.0](CHANGELOG.md#020),
    [v0.4.0](CHANGELOG.md#040), [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 5: Web Frontend

    **User-facing description to convey in prose:** The web application you open in a browser.
    Built on React 19, it gives you a visual dashboard for your entire job search: drag-and-drop
    Kanban board, scoring radar charts, discovery feed, analytics, and settings. All 13 pages
    in the current app cover the full workflow from onboarding to offer tracking.

    **Bullets to include (13 pages — verify count against frontend/src/pages/ before writing):**
    - Pipeline Kanban board: drag-and-drop cards across status columns
    - Application detail view: full scoring breakdown, activity log, notes
    - Discovery feed: browse scored matches, filter and sort, move to pipeline
    - Skills page: manage your skill profile
    - Analytics page: charts and stats across your job search
    - Follow-ups dashboard: see what needs attention today
    - Contacts page: relationship tracking
    - Learning page: development resources
    - AI Health Dashboard: provider status, cost tracking, cache hit rates
    - Settings page: configure AI providers, integrations, and preferences
    - Help page
    - Welcome/onboarding page
    - Radar chart for multi-dimension score breakdown
    - Red flag badges inline on job cards
    - Credits-exhausted banner when AI provider quota runs out
    - Incomplete profile warning with explanation of what's missing

    **Honest gaps to weave in:** The web app is designed for desktop width — no mobile-optimized
    layout yet. No dark mode. No offline mode — requires an active connection to the backend.

    **Release anchors:** [v0.1.0](CHANGELOG.md#010), [v0.2.0](CHANGELOG.md#020),
    [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 6: CLI & Packaging

    **User-facing description to convey in prose:** Kestrel has a terminal interface for power
    users and is distributed through multiple channels — pip, Docker, Homebrew, and npx — so
    it can run anywhere Python runs. The `kestrel` command exposes the full pipeline from the
    terminal.

    **Bullets to include:**
    - `kestrel` CLI (also aliased as `career`) with 5 subcommand groups: pipeline, skills, goals, interview-prep, contacts
    - WARN Act layoff data CLI command
    - Homebrew formula for macOS install (`brew install kestrel`)
    - npm wrapper package for `npx kestrel` install path
    - curl one-liner installer
    - PyPI distribution as `kestrel-app` (`pip install kestrel-app`)
    - Docker multi-stage build: Node 22 Alpine (frontend build) → Python 3.11 slim (runtime)
    - `docker compose up` for local development (two containers, hot-reload)
    - `docker compose -f docker-compose.prod.yml up` for single-container production
    - Deploy to Railway, Fly.io, or any VPS with included configs
    - Bundled frontend and migrations inside the pip wheel: self-contained single package
    - Auto-migration on startup: database schema always current when the server starts
    - GitHub Codespaces compatibility

    **Honest gaps to weave in (critical per ROADMAP.md context — deployment gap is the
    highest-priority forward milestone):** No native desktop installer — no `.dmg`, no `.exe`.
    You need Docker, Python, or a terminal to run Kestrel today. Docker was meant to be the
    easiest path, but had installation issues (now resolved, though Docker familiarity is still
    required). Three install methods exist but none are "download and open." This is the gap
    the app packaging milestone targets.

    **Release anchors:** [v0.1.0](CHANGELOG.md#010), [v0.2.0](CHANGELOG.md#020),
    [v0.6.0](CHANGELOG.md#060), [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 7: Infrastructure

    **User-facing description to convey in prose:** The engineering foundation that keeps Kestrel
    reliable and trustworthy. Every pull request runs through a multi-stage CI pipeline. Privacy
    enforcement, security scanning, and code quality gates run automatically. Users benefit from
    this even if they never see it — it's the reason Kestrel's scoring results are consistent
    and your data stays yours.

    **Bullets to include:**
    - GitHub Actions CI: Python lint, test, security audit on every PR
    - Frontend CI: ESLint, Vitest, npm audit, npm supply chain signature verification
    - SonarCloud quality analysis: 420+ code issues resolved during hardening
    - CodeQL static analysis
    - gitleaks secret scanning: prevents credential leaks in commits
    - PII audit scan in CI: catches accidental personal data in committed files
    - pip-audit and npm audit: dependency vulnerability scanning
    - 129+ automated tests (107 backend pytest files + 22 frontend Vitest files)
    - In-memory SQLite test isolation: tests never touch production data
    - Agent-aware test enforcement: tests can't hit real AI providers
    - Privacy layer: PII masking, provider privacy registry, GDPR metadata per provider
    - Encrypted AI response cache at rest (Fernet encryption)
    - Optional API key authentication middleware
    - Security hardening: log injection prevention, DoS bounds, path traversal mitigation
    - RFC 3339 UTC timestamp compliance across all API schemas

    **Honest gaps to weave in:** No structured log output (errors go to stdout/stderr only).
    No built-in database backup — your data lives in a single SQLite file that you're
    responsible for backing up.

    **Release anchors:** [v0.2.0](CHANGELOG.md#020), [v0.3.0](CHANGELOG.md#030),
    [v0.6.0](CHANGELOG.md#060), [v0.7.0](CHANGELOG.md#070), [v0.10.0](CHANGELOG.md#0100),
    [v0.11.0](CHANGELOG.md#0110)

    ---

    ### Domain 8: Integrations

    **User-facing description to convey in prose:** Kestrel connects to external services for
    notifications, task management, and calendar sync. All credentials are stored locally in
    your own database — they're never transmitted to any Kestrel server.

    **Bullets to include:**
    - TickTick task sync: auto-creates tasks from follow-up reminders, syncs completions back
    - TickTick background sync on a 15-minute interval
    - Pushover push notifications: discovery alerts, follow-up reminders, ghost alerts, interview reminders
    - Calendar export: iCal (.ics files), Google Calendar URL generation, Fantastical URL scheme
    - Integration management UI: connect/disconnect services, test connections, view credential status
    - OpenRouter OAuth PKCE: click "Connect" in Settings to provision your API key without manual copy/paste
    - Cloudflare Worker for bi-directional Linear↔TickTick sync and calendar feed generation
    - Google Sheets daily scan logging (optional)

    **Honest gaps to weave in:** Mailgun email notifications are configured (env vars exist)
    but not actively wired — no email delivery today. No Slack/Discord/Teams notification
    support. TickTick sync requires a TickTick account; no alternative task manager integration
    yet.

    **Release anchors:** [v0.3.0](CHANGELOG.md#030), [v0.4.0](CHANGELOG.md#040),
    [v0.11.0](CHANGELOG.md#0110)

    ---

    ## PARKED WORK SECTION (D-02, INV-04, INV-05)

    Write an H2 section: `## Parked Work`

    Brief intro paragraph: Some capabilities exist in the codebase but are not actively
    developed or fully functional. They're preserved here because they represent real
    investment and will resume at the right time.

    ### Mobile App subsection

    - React Native 0.81 / Expo 54 scaffold exists in the codebase
    - Tamagui 2.0 RC UI component library selected and installed
    - UX design research and mobile-specific findings documented in docs/ (extracted for web design insights)
    - Status: Development paused to prioritize web v1. The scaffold and UX findings are
      preserved. Mobile resumes when the web application is complete and stable. No tests
      exist for the mobile directory.

    ### Voice Mode subsection

    - API endpoint (voice.py) and a frontend page (VoiceDiscussion) exist in the codebase
    - The voice integration is registered in the integration system
    - Status: Untested. The end-to-end flow has not been validated. This is not a shipped
      feature. Before voice mode can be roadmapped as a planned milestone, it needs an audit
      to determine what works and what doesn't.

    ---

    ## FINAL PROSE QUALITY CHECKS

    Before writing, internalize these tone guidelines (D-05):
    - Write for a job seeker, not a developer
    - "The scoring engine learns what kinds of jobs you actually want" not "multi-factor rubric with 288 presets"
    - Explain WHY each capability matters to their job search, not WHAT it is technically
    - Honest gaps should read as useful information, not apologies
    - Avoid: "leverages", "robust", "seamlessly", "state-of-the-art"

    Target length: 2,000–3,000 words total for the whole document (per D-07).
    Each domain section should be ~150-250 words total (prose + bullets).
  </action>
  <verify>
    <automated>MISSING — this is a documentation phase with no automated test framework. Use the manual checklist below.</automated>
    Manual verification checklist:
    1. `test -f docs/roadmap/inventory.md && echo "file exists"` — file was created
    2. `wc -w docs/roadmap/inventory.md` — word count should be 1,800–3,500 (markdown overhead included)
    3. `grep -c "^## " docs/roadmap/inventory.md` — should be 10 or 11 (8 domains + Parked Work + At a Glance)
    4. `grep "## Parked Work" docs/roadmap/inventory.md` — Parked Work section exists
    5. `grep "Untested" docs/roadmap/inventory.md` — Voice mode is framed as untested
    6. `grep -c "CHANGELOG.md#" docs/roadmap/inventory.md` — at least 8 CHANGELOG links (one per domain)
    7. `grep "## AI Provider System\|## Scoring Engine\|## Discovery Engine\|## Application Pipeline\|## Web Frontend\|## CLI & Packaging\|## Infrastructure\|## Integrations" docs/roadmap/inventory.md | wc -l` — should be 8
    8. Read the "Parked Work" section — confirm Mobile says "paused" and Voice says "untested, needs audit"
    9. Read the opening narrative — confirm it covers CLI-tool origin through v0.11.0 hardening
    10. Read the CLI & Packaging domain — confirm no-desktop-installer gap is clearly stated
  </verify>
  <done>
    docs/roadmap/inventory.md exists with:
    - Opening evolution narrative of 2-3 paragraphs covering CLI-tool origin through v0.11.0
    - Summary table listing all 8 domains + 2 parked items with Shipped/Parked status
    - 8 domain sections each containing prose + bullet list + CHANGELOG cross-links
    - Parked Work section with Mobile (paused) and Voice (untested) subsections
    - All 9 AI providers listed in AI Provider System domain (including v0.11.0 additions)
    - Honest user-facing gaps woven inline into domain prose (no separate "Gaps" heading)
    - Total word count approximately 2,000–3,000 words
    - Warm teaching tone throughout — written for job seekers, not developers
    - No file paths, API routes, or internal architecture details (per D-08)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inventory content → public GitHub | This document will be publicly visible; any internal paths, personal data, or non-public information included accidentally would be a disclosure |
| Source docs → inventory claims | The executor translates internal codebase analysis docs into public-facing prose; hallucinated or unverified feature claims damage credibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | inventory.md content | mitigate | Do not include internal file paths, API routes, database schema details, or architecture-layer terminology; D-08 explicitly forbids this. The read_first list includes CONCERNS.md for gap awareness only — none of its internal tech debt content should appear in the output. |
| T-01-02 | Information Disclosure | CHANGELOG.md anchor links | accept | CHANGELOG.md is a public file in the repo; linking to it discloses nothing non-public. Low risk. |
| T-01-03 | Spoofing (accuracy) | Feature claims in inventory | mitigate | Every capability listed must be cross-referenced against at least one source doc (ARCHITECTURE.md, STRUCTURE.md, INTEGRATIONS.md, or CHANGELOG.md). The research doc's known-stale sections are called out in Critical Corrections — executor must not use the stale Groq/xAI/OpenAI/Gemini framing. |
| T-01-04 | Repudiation (false claims) | Voice mode framing | mitigate | Voice mode MUST be framed as "untested, needs audit" not as a shipped feature. This is a factual accuracy requirement, not an editorial choice. Claiming a broken/untested feature as shipped damages user trust. |
| T-01-05 | Information Disclosure | Private project docs | mitigate | The docs/research/ and private/ directories contain business strategy, competitive intel, and non-public research. None of this should be referenced or quoted in inventory.md. Source docs are limited to .planning/codebase/, CHANGELOG.md, docs/reference/, and pyproject.toml. |
</threat_model>

<verification>
Phase 1 is complete when:

1. `test -f docs/roadmap/inventory.md` exits 0
2. `grep -c "^## " docs/roadmap/inventory.md` returns >= 10 (summary table + 8 domains + Parked Work)
3. `grep "## Parked Work" docs/roadmap/inventory.md` matches
4. `grep "Untested\|untested" docs/roadmap/inventory.md` matches (Voice mode framing)
5. `grep -c "CHANGELOG.md#" docs/roadmap/inventory.md` returns >= 8
6. `wc -w docs/roadmap/inventory.md` shows >= 1,500 words
7. Manual read confirms:
   - Warm teaching tone throughout (no developer jargon, explains WHY not just WHAT)
   - All 9 AI providers mentioned in AI Provider System domain
   - Cost presets (Free/Budget/Quality/Private/Custom) mentioned
   - Onboarding wizard mentioned in Application Pipeline
   - No-desktop-installer gap called out in CLI & Packaging
   - Mailgun "configured but not wired" noted in Integrations
   - Evolution narrative spans CLI tool through v0.11.0 hardening
</verification>

<success_criteria>
- docs/roadmap/ directory exists
- docs/roadmap/inventory.md exists with approximately 2,000–3,000 words
- 8 domain sections present, all with warm prose + bullets + CHANGELOG cross-links
- Parked Work section present with Mobile (paused) and Voice (untested) correctly framed
- All 9 AI providers listed, including v0.11.0 additions (Groq, OpenAI, xAI, Gemini)
- v0.11.0 features reflected: cost presets, batch scoring, onboarding wizard, pre-filter integration
- No internal tech debt, file paths, or API routes in the document
- Opening evolution narrative covers the full arc from CLI tool through v0.11.0
- Summary table at top with all 10 rows (8 domains + Mobile + Voice)
</success_criteria>

<output>
After completion, create `.planning/phases/01-feature-inventory/01-01-SUMMARY.md` using the
summary template at @$HOME/.claude/get-shit-done/templates/summary.md.

The summary should capture:
- What was created (docs/roadmap/inventory.md, docs/roadmap/ directory)
- Key editorial decisions made (section ordering, tone choices, how gaps were framed)
- Any corrections applied to stale research doc claims (v0.11.0 providers, anchor format)
- Actual word count and section counts
- Cross-references to Phase 2 (which reads this file to assemble ROADMAP.md)
</output>
