# Private local-only notes (never commit)
private/
docs/sessions/

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/
*.egg
.testmondata
venv/
.venv/
.venv-career/
env/
uv.lock

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.local
.env.production
.env.staging
.env.*.local
langfuse.env
*.key
credentials.json

# Personal config (generated from examples)
config/personal.yaml

# Cursor / agent internals
.cursor/
agent-transcripts/
mcps/
.memory/

# MCP virtual environments
.venv_mcp/

# Agent/MCP config files (local only)
.clinerules
.cursorrules
.contextstream/
.kilocode/
.mcp.json
AGENTS.md
GEMINI.md
GSD-CLAUDE.md

# Pre-existing tool scripts (not part of this repo)
tools/mcp_sanity_check.py

# Node
node_modules/

# Dashboard build output (regenerated at deploy time)
dashboard/data.json

# CV build intermediates (PDF/DOCX/HTML are committed as releases)
cv/rendercv_output/

# Career OS data (SQLite, local state)
data/

# Scraped/scored job data (generated locally)
tracking/*.json

# Temp
tmp/
*.tmp
*.log

# Third-party MCP (cloned, re-clone if needed)
jobspy-mcp-server/
.browser-profile/
.claude/worktrees/

# Coverage reports (generated in CI)
coverage.xml
.coverage
htmlcov/
frontend/coverage/
lcov.info

# BMAD (local dev tooling, not shipped to users)
_bmad-output/
_bmad/

# GSD (local dev tooling, not shipped to users)
.planning/
GSD-CLAUDE.md
.claude/commands/gsd/
.claude/get-shit-done/
.claude/agents/gsd-*
.claude/hooks/gsd-*
gsd-local-patches/

# Internal business docs
docs/pricing-strategy.md
